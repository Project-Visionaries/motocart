import React from 'react'
import { CarouselAnimation } from '../../utils/Animations/Carousel'

interface Props {
    
}

export const TestingPage = (props: Props) => {
    return (
        <div>
            <CarouselAnimation />
        </div>
    )
}
